# Where the "Register Your Site" form submissions are delivered.
# Leave unset in dev — submissions are logged to the server console instead.
REGISTRATION_NOTIFY_EMAIL=info@orcatrial.net

# Optional: forward submissions to an external endpoint (Zapier / Make / CRM webhook).
REGISTRATION_WEBHOOK_URL=
